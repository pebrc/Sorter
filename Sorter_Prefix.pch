//
// Prefix header for all source files of the 'Sorter' target in the 'Sorter' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>
#endif
#if MY_DEBUG_FLAG
#define DEBUG_OUTPUT( a, b )     NSLog( a, b )
#else
#define DEBUG_OUTPUT( a, b )     // (a,b)
#endif